
var {stringify} = require("../");
var assert  = require("assert");

describe('test index.js', function() {
    it('output should equals ', function() {
       if(false) {
         throw new Error("the ouput is not what we expacted !");
       }
    });
});

